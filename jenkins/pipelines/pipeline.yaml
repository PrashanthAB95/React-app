pipeline {
    agent any

    stages {
        stage('Checkout Source Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/PrashanthAB95/React-app.git',
                    credentialsId: 'github-pat'
            }
        }
    }
}